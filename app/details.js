import { View, Text } from 'react-native'
import React from 'react'

const Details = ({data}) => {
  console.log(data);
  return (
    <View>
      <Text>Details</Text>
    </View>
  )
}

export default Details